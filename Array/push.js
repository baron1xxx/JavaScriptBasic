/**
 * Додає елементи в укінець масива.
 * return нову довжину масиву.
 */

// arr.push(element1, ..., elementN)
let arr = ['a', 'b', 'c'];

console.log(arr.push('d', 'e')); // 5.
console.log(arr);// [ 'a', 'b', 'c', 'd', 'e' ]
console.log([].push('d', 'e')); // 2.
