/**
 * Удаляє останній елемент масива.
 * return елемент який видалено.
 */

// arr.pop()
const arr = ['a', 'b', 'c'];

console.log(arr.pop()); // c.
console.log([].pop()); // undefined.
 // ****!!!!! Міняє початковий масив !!!!********
console.log(arr); // [ 'a', 'b' ]
