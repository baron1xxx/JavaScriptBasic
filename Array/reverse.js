/**
 * Перевертає масив(міняє послідовність елементів на протилежну).
 * return array
 */

// arr.reverse()

let arr = ['a', 'b', 'c'];
const reverseArray = arr.reverse();
console.log(reverseArray); // [ 'c', 'b', 'a' ]
console.log(arr); // [ 'c', 'b', 'a' ]
